use std::collections::HashMap;
use std::fs;
use std::io::Write;
use std::path::{Path, PathBuf};

fn main() {
    // Check if CodeGraph is initialized
    if !Path::new("../.codegraph").exists() {
        println!("cargo:warning=CodeGraph is not initialized. For faster code exploration, run:");
        println!("cargo:warning=  bunx @colbymchenry/codegraph");
        println!("cargo:warning=  codegraph init");
        println!("cargo:warning=  codegraph index");
    }

    // Emit build timestamp (UTC) as a compile-time env var
    let now = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .unwrap()
        .as_secs();
    // Format as a human-readable UTC timestamp without pulling in chrono
    let secs = now;
    let days_since_epoch = secs / 86400;
    let time_of_day = secs % 86400;
    let hours = time_of_day / 3600;
    let minutes = (time_of_day % 3600) / 60;
    let seconds = time_of_day % 60;

    // Compute year/month/day from days since 1970-01-01 (civil calendar algorithm)
    let z = days_since_epoch as i64 + 719468;
    let era = z / 146097;
    let doe = z - era * 146097;
    let yoe = (doe - doe / 1460 + doe / 36524 - doe / 146096) / 365;
    let y = yoe + era * 400;
    let doy = doe - (365 * yoe + yoe / 4 - yoe / 100);
    let mp = (5 * doy + 2) / 153;
    let d = doy - (153 * mp + 2) / 5 + 1;
    let m = if mp < 10 { mp + 3 } else { mp - 9 };
    let y = if m <= 2 { y + 1 } else { y };

    let timestamp = format!(
        "{:04}-{:02}-{:02} {:02}:{:02}:{:02} UTC",
        y, m, d, hours, minutes, seconds
    );
    println!("cargo:rustc-env=BUILD_TIMESTAMP={}", timestamp);

    // Re-run if source changes
    println!("cargo:rerun-if-changed=src/program");
    println!("cargo:rerun-if-changed=src/rosy_lib");

    // Generate the embedded files at compile time
    let out_dir = std::env::var("OUT_DIR").unwrap();
    let dest_path = Path::new(&out_dir).join("embedded_rosy_lib.rs");
    let mut f = fs::File::create(&dest_path).unwrap();

    writeln!(f, "// Auto-generated by build.rs - do not edit manually!").unwrap();
    writeln!(f, "").unwrap();
    writeln!(f, "pub struct EmbeddedFile {{").unwrap();
    writeln!(f, "    pub path: &'static str,").unwrap();
    writeln!(f, "    pub content: &'static str,").unwrap();
    writeln!(f, "}}").unwrap();
    writeln!(f, "").unwrap();
    writeln!(f, "pub const ROSY_LIB_FILES: &[EmbeddedFile] = &[").unwrap();

    // Walk the rosy_lib directory
    let lib_path = PathBuf::from("src/rosy_lib");
    let files = collect_files(&lib_path, &lib_path);

    let _file_count = files.len();

    for file in files {
        let full_path = lib_path.join(&file);
        writeln!(f, "    EmbeddedFile {{").unwrap();
        writeln!(f, "        path: r#\"{}\"#,", file.display()).unwrap();
        writeln!(
            f,
            "        content: include_str!(r#\"{}\"#),",
            full_path.canonicalize().unwrap().display()
        )
        .unwrap();
        writeln!(f, "    }},").unwrap();
    }

    writeln!(f, "];").unwrap();

    // ─── LSP: Generate keyword list and hover docs from grammar + modules ──
    let pest_path = Path::new("assets/rosy.pest");
    let program_dir = Path::new("src/program");
    println!("cargo:rerun-if-changed={}", pest_path.display());

    let pest_source = fs::read_to_string(pest_path).expect("Failed to read rosy.pest");
    let keywords = extract_keywords(&pest_source);
    let module_docs = scan_module_docs(program_dir);
    let rule_to_keyword = build_rule_keyword_map(&pest_source);
    generate_keywords_file(&out_dir, &keywords, &module_docs);
    generate_hover_file(&out_dir, &module_docs, &rule_to_keyword);
}

fn collect_files(root: &Path, current: &Path) -> Vec<PathBuf> {
    let mut files = Vec::new();

    if let Ok(entries) = fs::read_dir(current) {
        for entry in entries.flatten() {
            let path = entry.path();

            // Skip target directory
            if path.file_name().map_or(false, |n| n == "target") {
                continue;
            }

            if path.is_dir() {
                files.extend(collect_files(root, &path));
            } else if path.is_file() {
                // Include .rs, .rosy, .fox, and .md files
                if let Some(ext) = path.extension() {
                    if ext == "rs" || ext == "rosy" || ext == "fox" || ext == "md" {
                        if let Ok(relative) = path.strip_prefix(root) {
                            files.push(relative.to_path_buf());
                        }
                    }
                }
            }
        }
    }

    files.sort();
    files
}

// ─── LSP: Keyword & Hover Doc Generation ───────────────────────────────────

const BASE_DOC_URL: &str = "https://hiibolt.github.com/rosy/rosy";

fn extract_keywords(source: &str) -> Vec<String> {
    let mut keywords = Vec::new();
    for line in source.lines() {
        let trimmed = line.trim();
        if trimmed.starts_with("keyword_raw") {
            if let Some(start) = trimmed.find('{') {
                let body = &trimmed[start + 1..];
                let body = body.trim_end_matches('}').trim();
                for part in body.split('|') {
                    if let Some(s) = extract_quoted(part.trim()) {
                        let upper = s.to_uppercase();
                        if !upper.is_empty()
                            && upper != "TRUE"
                            && upper != "FALSE"
                            && !upper.starts_with("ROSY_")
                        {
                            keywords.push(upper);
                        }
                    }
                }
            }
        }
    }
    keywords.sort();
    keywords.dedup();
    keywords
}

fn extract_quoted(s: &str) -> Option<String> {
    let s = s.trim().trim_start_matches('^');
    if s.starts_with('"') && s.ends_with('"') && s.len() >= 2 {
        Some(s[1..s.len() - 1].to_string())
    } else {
        None
    }
}

struct ModuleDoc {
    keyword: String,
    title: String,
    description: String,
    doc_url: String,
    kind: ModuleKind,
}

#[derive(Clone, Copy)]
enum ModuleKind {
    Statement,
    Expression,
}

fn scan_module_docs(program_dir: &Path) -> HashMap<String, ModuleDoc> {
    let mut docs = HashMap::new();
    for (subdir, kind) in [
        ("statements", ModuleKind::Statement),
        ("expressions", ModuleKind::Expression),
    ] {
        let dir = program_dir.join(subdir);
        if dir.is_dir() {
            scan_modules_recursive(&dir, &dir, kind, &mut docs);
        }
    }
    docs
}

fn scan_modules_recursive(
    base_dir: &Path,
    current_dir: &Path,
    kind: ModuleKind,
    docs: &mut HashMap<String, ModuleDoc>,
) {
    let Ok(entries) = fs::read_dir(current_dir) else { return };
    for entry in entries.flatten() {
        let path = entry.path();
        if path.is_dir() {
            let mod_file = path.join("mod.rs");
            if mod_file.is_file() {
                if let Some(doc) = parse_module_doc(&mod_file, &path, base_dir, kind) {
                    docs.insert(doc.keyword.clone(), doc);
                }
            }
            scan_modules_recursive(base_dir, &path, kind, docs);
        }
    }
}

fn parse_module_doc(
    mod_file: &Path,
    module_dir: &Path,
    base_dir: &Path,
    kind: ModuleKind,
) -> Option<ModuleDoc> {
    let content = fs::read_to_string(mod_file).ok()?;
    let dir_name = module_dir.file_name()?.to_str()?;
    let keyword = dir_name_to_keyword(dir_name)?;

    let mut doc_lines = Vec::new();
    for line in content.lines() {
        let trimmed = line.trim();
        if trimmed.starts_with("//!") {
            doc_lines.push(trimmed.trim_start_matches("//!").trim().to_string());
        } else if !trimmed.is_empty() {
            break;
        }
    }
    if doc_lines.is_empty() { return None; }

    let title = doc_lines[0].trim_start_matches('#').trim().to_string();
    let mut desc_parts = Vec::new();
    let mut started = false;
    for line in &doc_lines[1..] {
        if line.is_empty() { if started { break; } continue; }
        if line.starts_with('#') { break; }
        started = true;
        desc_parts.push(line.as_str());
    }
    let description = desc_parts.join(" ");

    let relative = module_dir.strip_prefix(base_dir).ok()?;
    let kind_prefix = match kind {
        ModuleKind::Statement => "program/statements",
        ModuleKind::Expression => "program/expressions",
    };
    let doc_url = format!(
        "{BASE_DOC_URL}/{kind_prefix}/{}/",
        relative.display().to_string().replace('\\', "/")
    );

    Some(ModuleDoc { keyword, title, description, doc_url, kind })
}

fn dir_name_to_keyword(dir_name: &str) -> Option<String> {
    let keyword = match dir_name {
        "var_decl" => "VARIABLE",
        "var_expr" | "variable_identifier" | "assign" | "function_call" | "procedure_call" => return None,
        "while_loop" => "WHILE",
        "da_init" => "DAINI",
        "break" => "BREAK", "if" => "IF", "loop" => "LOOP", "ploop" => "PLOOP",
        "function" => "FUNCTION", "procedure" => "PROCEDURE", "quit" => "QUIT",
        "os_call" => "OS",
        "cos_fn" | "cos" => "COS", "sin" => "SIN", "tan_fn" | "tan" => "TAN",
        "asin_fn" | "asin" => "ASIN", "acos_fn" | "acos" => "ACOS", "atan_fn" | "atan" => "ATAN",
        "sinh_fn" | "sinh" => "SINH", "cosh_fn" | "cosh" => "COSH", "tanh_fn" | "tanh" => "TANH",
        "sqrt_fn" | "sqrt" => "SQRT", "exp_fn" | "exp" => "EXP", "log_fn" | "log" => "LOG",
        "abs_fn" | "abs" => "ABS", "norm_fn" | "norm" => "NORM", "cons_fn" | "cons" => "CONS",
        "int_fn" => "INT", "nint" => "NINT", "type_fn" => "TYPE",
        "real_fn" => "REAL", "imag_fn" => "IMAG",
        "re_convert" => "RE", "string_convert" => "ST", "logical_convert" => "LO",
        "complex_convert" => "CM", "ve_convert" => "VE",
        "erf" => "ERF", "werf" => "WERF", "isrt" => "ISRT", "isrt3" => "ISRT3",
        "cmplx" => "CMPLX", "conj" => "CONJ", "trim" => "TRIM", "ltrim" => "LTRIM",
        "length" => "LENGTH", "varmem" => "VARMEM", "varpoi" => "VARPOI",
        "pow" | "neg" | "not" => return None,
        "add" | "sub" | "mult" | "div" => return None,
        "eq" | "neq" | "lt" | "gt" | "lte" | "gte" => return None,
        "concat" | "derive" | "extract" => return None,
        "number" | "string" | "boolean" => return None,
        "cd" => "CD", "da" => "DA",
        "core" | "io" | "math" | "trig" | "exponential" | "rounding"
        | "special" | "vector" | "complex" | "memory" | "query"
        | "conversion" | "sys" | "collection" | "comparison"
        | "arithmetic" | "unary" | "types" | "operators" | "functions" => return None,
        other => return Some(other.to_uppercase()),
    };
    Some(keyword.to_string())
}

fn build_rule_keyword_map(source: &str) -> HashMap<String, String> {
    let mut map = HashMap::new();
    for line in source.lines() {
        let trimmed = line.trim();
        if let Some((rule_name, rest)) = trimmed.split_once('=') {
            let rule_name = rule_name.trim();
            if let Some(kw) = extract_first_keyword(rest) {
                map.insert(rule_name.to_string(), kw.to_uppercase());
            }
        }
    }
    map
}

fn extract_first_keyword(body: &str) -> Option<String> {
    let body = body.trim().trim_start_matches('{').trim();
    if let Some(pos) = body.find("^\"") {
        let rest = &body[pos + 2..];
        if let Some(end) = rest.find('"') {
            return Some(rest[..end].to_string());
        }
    }
    None
}

fn generate_keywords_file(out_dir: &str, keywords: &[String], module_docs: &HashMap<String, ModuleDoc>) {
    let dest = Path::new(out_dir).join("keywords_generated.rs");
    let mut f = fs::File::create(&dest).unwrap();
    writeln!(f, "// Auto-generated from rosy.pest + module docs by build.rs — do not edit!").unwrap();
    writeln!(f).unwrap();
    writeln!(f, "pub const ROSY_KEYWORD_LIST: &[(&str, &str)] = &[").unwrap();
    for kw in keywords {
        let desc = module_docs.get(kw.as_str())
            .map(|d| if d.description.is_empty() { d.title.clone() } else { d.description.clone() })
            .unwrap_or_else(|| "Rosy keyword".to_string());
        let desc = desc.replace('\\', "\\\\").replace('"', "\\\"");
        writeln!(f, "    (\"{kw}\", \"{desc}\"),").unwrap();
    }
    writeln!(f, "];").unwrap();
}

fn generate_hover_file(out_dir: &str, module_docs: &HashMap<String, ModuleDoc>, _rule_to_keyword: &HashMap<String, String>) {
    let dest = Path::new(out_dir).join("hover_generated.rs");
    let mut f = fs::File::create(&dest).unwrap();
    writeln!(f, "// Auto-generated from module doc comments by build.rs — do not edit!").unwrap();
    writeln!(f).unwrap();
    writeln!(f, "pub const ROSY_HOVER_DOCS: &[(&str, &str, &str, &str, bool)] = &[").unwrap();
    let mut entries: Vec<_> = module_docs.iter().collect();
    entries.sort_by_key(|(k, _)| (*k).clone());
    for (keyword, doc) in &entries {
        let title = doc.title.replace('\\', "\\\\").replace('"', "\\\"");
        let desc = doc.description.replace('\\', "\\\\").replace('"', "\\\"");
        let url = &doc.doc_url;
        let is_stmt = matches!(doc.kind, ModuleKind::Statement);
        writeln!(f, "    (\"{keyword}\", \"{title}\", \"{desc}\", \"{url}\", {is_stmt}),").unwrap();
    }
    writeln!(f, "];").unwrap();

    writeln!(f).unwrap();
    writeln!(f, "pub const ROSY_TYPE_HOVER: &[(&str, &str, &str)] = &[").unwrap();
    let base = BASE_DOC_URL;
    for (name, rust_type, desc) in [
        ("RE", "f64", "Real number"), ("ST", "String", "String"),
        ("LO", "bool", "Logical / boolean"), ("CM", "Complex64", "Complex number"),
        ("VE", "Vec<f64>", "Vector"), ("DA", "Taylor series", "Differential Algebra"),
        ("CD", "Complex Taylor series", "Complex Differential Algebra"),
    ] {
        writeln!(f, "    (\"{name}\", \"**{name}** \\u{{2014}} {desc} (`{rust_type}`)\\n\\n[Documentation]({base}/rosy_lib/enum.RosyBaseType.html#variant.{name})\", \"{desc}\"),").unwrap();
    }
    writeln!(f, "];").unwrap();
}
