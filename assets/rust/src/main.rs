mod cosy;

use cosy::*;

/*
fn add_two_numbers (a: &Cosy, b: &Cosy) -> Cosy {
    let add_two_numbers;
    add_two_numbers = (a + b).concat(a);
    add_two_numbers
}

fn main() {
    let cosy_val_1 = Cosy::Real(1.0);
    let cosy_val_2 = Cosy::Real(3.0);

    let result = add_two_numbers(&cosy_val_1, &cosy_val_2);
    let result = add_two_numbers(&result, &Cosy::Vector(vec![1.0, 2.0]));

    println!("The sum is: {}", result);
}
*/