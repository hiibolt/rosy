mod operators;
mod intrinsics;

pub type RE = f64;
pub type ST = String;
pub type LO = bool;
pub type CE = (f64, f64);
pub type VE = Vec<f64>;

/*
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
 */